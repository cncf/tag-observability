title: Prometheus Query Language

abbreviation: PromQL

copyright: The Prometheus Authors

license: Apache License V2

isOSS: true

paradigms:
  - functional
  - declarative

supportedTelemetryTypes:
  - type: metrics
    callouts:
      - title: Default Expansion
        description: >
          Data matching the predicate is returned as individual time series by
          default. Explicit grouping functions must be supplied to aggregate 
          time series into a single result.

inspirations:
  - Borgmon from Google

description: >
  A functional language with pipeline support for metric analysis.

designCallouts: TODO

caveats: TODO

references:
  - https://prometheus.io/docs/prometheus/latest/querying/basics/
  - https://sre.google/sre-book/practical-alerting/#:~:text=Borgmon